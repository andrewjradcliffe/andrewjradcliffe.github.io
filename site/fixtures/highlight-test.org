#+SETUPFILE: ../setupfile.org
#+TITLE: Highlight test

Fixture exercising the Zone htmlize theme across languages (Sub-spec A).

#+begin_src rust
// a comment
fn zone(n: usize) -> Result<f64, Error> {
    let ocean = 0x4e8d86;
    "a string literal"
}
#+end_src

#+begin_src julia
# a comment
function whiten(X::Matrix{Float64})
    μ = sum(X) / length(X)
    return X .- μ
end
#+end_src

#+begin_src python
# a comment
def logsumexp(xs: list[float]) -> float:
    m = max(xs)
    return m + log(sum(exp(x - m) for x in xs))
#+end_src

#+begin_src sh
# a comment
for f in *.org; do
    emacs --batch "$f" && echo "ok"
done
#+end_src
